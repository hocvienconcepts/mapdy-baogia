# Mapdy — Báo giá Founder & đội mở điểm (v1.17)

Trang báo giá tĩnh (1 file `index.html`) cho nền tảng phân tích vị trí Mapdy.
Deploy bằng Cloudflare Pages qua kết nối GitHub.

## Nội dung
- `index.html` — toàn bộ trang (HTML + CSS + JS inline, chạy độc lập, không cần build).

## Deploy
Xem `HUONG_DAN_DEPLOY.md` để nối repo này với Cloudflare Pages và lấy link `*.pages.dev`.

> "Không mở quán để thử — mở để sống được, và sống dài lâu." — VCS
